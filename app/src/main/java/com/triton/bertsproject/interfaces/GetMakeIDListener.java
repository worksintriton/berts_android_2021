package com.triton.bertsproject.interfaces;

import android.widget.CheckBox;

public interface GetMakeIDListener {
    void getMakeIDListener(String id, String make_name, CheckBox cb_flist, boolean isChecked);
}
